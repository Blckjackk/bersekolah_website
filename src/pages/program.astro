---
import Navbar from "../component/Header";
import AppLayout from "../layout/app-layout.astro";
---

<AppLayout>
  <!-- Hero Section -->

  <!-- Hero Banner -->
    <section class="relative flex items-center justify-center min-h-screen overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-br from-slate-900/95 via-[#406386]/90 to-[#406386]/85"></div>
        <div 
            class="absolute inset-0 bg-center bg-no-repeat bg-cover opacity-30" 
            style="background-image: url('assets/image/IMG_3694.jpg');"
        ></div>
        
        <div class="container relative z-10 px-4 mx-auto text-center text-white sm:px-6 lg:px-8">
            <div class="max-w-5xl mx-auto">
                <h1 class="mb-4 text-3xl font-bold leading-tight sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl sm:mb-6">
                    Program Beasiswa 
                    <span class="block mt-2 text-transparent bg-clip-text bg-gradient-to-r from-cyan-300 to-blue-200">
                        Bersekolah
                    </span>
                </h1>
                <p class="max-w-4xl mx-auto mb-6 text-base font-light leading-relaxed sm:text-lg md:text-xl lg:text-2xl opacity-90 sm:mb-8">
                    Memberikan kesempatan pendidikan yang lebih baik bagi pelajar SMP-SMA yang memiliki semangat tinggi untuk bersekolah.                </p>                <div class="mb-6 sm:mb-8">                    <div class="max-w-xl px-4 py-5 mx-auto">
                        <h3 class="mb-4 text-xl font-bold text-center text-white sm:text-2xl">
                            Pendaftaran Ditutup Dalam:
                        </h3>
                        <div id="countdown-container" class="flex items-center justify-center">
                            <!-- Loading State -->
                            <div class="flex flex-col items-center p-6 countdown-loading">
                                <div class="w-8 h-8 mb-2 border-t-2 border-r-2 border-blue-300 rounded-full animate-spin"></div>
                                <p class="text-sm text-white">Memuat data periode...</p>
                            </div>
                            
                            <!-- Active Countdown Display -->
                            <div class="hidden countdown-display">
                                <div class="flex items-center justify-center space-x-4 sm:space-x-6">
                                    <!-- Days -->
                                    <div class="flex flex-col items-center">
                                        <div class="flex items-center justify-center w-16 h-16 border-2 border-blue-100 shadow-lg bg-white/90 backdrop-blur-sm sm:w-20 sm:h-20 rounded-xl">
                                            <span id="days" class="text-2xl sm:text-3xl font-bold text-[#406386]">00</span>
                                        </div>
                                        <span class="mt-2 text-xs font-medium text-white sm:text-sm">Hari</span>
                                    </div>
                                    
                                    <!-- Separator -->
                                    <div class="flex flex-col items-center justify-center text-2xl font-bold text-white">
                                        <div class="flex items-center h-16 sm:h-20">:</div>
                                        <div class="h-4 mt-2"></div>
                                    </div>
                                    
                                    <!-- Hours -->
                                    <div class="flex flex-col items-center">
                                        <div class="flex items-center justify-center w-16 h-16 border-2 border-blue-100 shadow-lg bg-white/90 backdrop-blur-sm sm:w-20 sm:h-20 rounded-xl">
                                            <span id="hours" class="text-2xl sm:text-3xl font-bold text-[#406386]">00</span>
                                        </div>
                                        <span class="mt-2 text-xs font-medium text-white sm:text-sm">Jam</span>
                                    </div>
                                    
                                    <!-- Separator -->
                                    <div class="flex flex-col items-center justify-center text-2xl font-bold text-white">
                                        <div class="flex items-center h-16 sm:h-20">:</div>
                                        <div class="h-4 mt-2"></div>
                                    </div>
                                    
                                    <!-- Minutes -->
                                    <div class="flex flex-col items-center">
                                        <div class="flex items-center justify-center w-16 h-16 border-2 border-blue-100 shadow-lg bg-white/90 backdrop-blur-sm sm:w-20 sm:h-20 rounded-xl">
                                            <span id="minutes" class="text-2xl sm:text-3xl font-bold text-[#406386]">00</span>
                                        </div>
                                        <span class="mt-2 text-xs font-medium text-white sm:text-sm">Menit</span>
                                    </div>
                                    
                                    <!-- Separator -->
                                    <div class="flex flex-col items-center justify-center text-2xl font-bold text-white">
                                        <div class="flex items-center h-16 sm:h-20">:</div>
                                        <div class="h-4 mt-2"></div>
                                    </div>
                                    
                                    <!-- Seconds -->
                                    <div class="flex flex-col items-center">
                                        <div class="flex items-center justify-center w-16 h-16 border-2 border-blue-100 shadow-lg bg-white/90 backdrop-blur-sm sm:w-20 sm:h-20 rounded-xl">
                                            <span id="seconds" class="text-2xl sm:text-3xl font-bold text-[#406386]">00</span>
                                        </div>
                                        <span class="mt-2 text-xs font-medium text-white sm:text-sm">Detik</span>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Expired Registration State -->
                            <div class="hidden countdown-expired">
                                <div class="px-8 py-4 border-2 border-red-100 shadow-lg bg-white/90 backdrop-blur-sm rounded-xl">
                                    <div class="flex items-center space-x-3">
                                        <svg class="w-6 h-6 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                        </svg>
                                        <p class="text-[#406386] font-bold text-lg">Pendaftaran telah ditutup</p>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Error State -->
                            <div class="hidden countdown-error">
                                <div class="px-8 py-4 border-2 border-red-100 shadow-lg bg-white/90 backdrop-blur-sm rounded-xl">
                                    <div class="flex items-center space-x-3">
                                        <svg class="w-6 h-6 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                                        </svg>
                                        <p class="text-lg font-bold text-red-600">Tidak ada periode aktif</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>                <script>
                    document.addEventListener('DOMContentLoaded', function() {
                        // Type casting DOM elements to avoid null warnings
                        const countdownLoading = document.querySelector('.countdown-loading') as HTMLElement | null;
                        const countdownDisplay = document.querySelector('.countdown-display') as HTMLElement | null;
                        const countdownExpired = document.querySelector('.countdown-expired') as HTMLElement | null;
                        const countdownError = document.querySelector('.countdown-error') as HTMLElement | null;
                        
                        const daysElement = document.getElementById('days') as HTMLElement | null;
                        const hoursElement = document.getElementById('hours') as HTMLElement | null;
                        const minutesElement = document.getElementById('minutes') as HTMLElement | null;
                        const secondsElement = document.getElementById('seconds') as HTMLElement | null;
                        
                        // Safety check for DOM elements
                        if (!countdownLoading || !countdownDisplay || !countdownExpired || !countdownError ||
                            !daysElement || !hoursElement || !minutesElement || !secondsElement) {
                            console.error('Required DOM elements not found for countdown');
                            return;
                        }
                        
                        // Type definitions for API response
                        interface CountdownResponse {
                            days: number;
                            hours: number;
                            minutes: number;
                            seconds: number;
                            end_date?: string;
                            status?: string;
                            message?: string;
                            tahun?: number;
                            nama?: string;
                        }
                        
                        // API endpoint for countdown data
                        const API_URL = '/api/countdown/pendaftaran';
                        
                        // Fetch countdown data from API
                        fetch(API_URL)
                            .then(response => {
                                if (!response.ok) {
                                    throw new Error(`Network response was not ok: ${response.status} ${response.statusText}`);
                                }
                                return response.json() as Promise<CountdownResponse>;
                            })
                            .then((data: CountdownResponse) => {
                                console.log('Countdown data:', data);
                                
                                // Hide loading indicator
                                countdownLoading.classList.add('hidden');
                                  // Handle expired registration or no active period
                                if (!data) {
                                    countdownError.classList.remove('hidden');
                                    return;
                                }
                                
                                // Check for expired registration (all zeros or specific message)
                                if ((data.days === 0 && data.hours === 0 && data.minutes === 0 && data.seconds === 0) || 
                                    data.message === 'Pendaftaran sudah ditutup.') {
                                    countdownExpired.classList.remove('hidden');
                                    return;
                                }
                                
                                // Check for error status or message
                                if (data.message === 'Tidak ada periode beasiswa yang aktif saat ini.' || data.status === 'error') {
                                    countdownError.classList.remove('hidden');
                                    return;
                                }
                                
                                // Extract end date from API response
                                const endDateStr = data.end_date;
                                if (!endDateStr) {
                                    console.error('No end date provided in API response');
                                    countdownError.classList.remove('hidden');
                                    return;
                                }
                                
                                const endDate = new Date(endDateStr);
                                
                                // Check if end date is valid
                                if (isNaN(endDate.getTime())) {
                                    console.error('Invalid end date:', endDateStr);
                                    countdownError.classList.remove('hidden');
                                    return;
                                }
                                
                                // Initialize countdown with either API-provided values or calculated ones
                                if (data.days !== undefined && data.hours !== undefined && 
                                    data.minutes !== undefined && data.seconds !== undefined) {
                                    // Use API-provided values for immediate display
                                    updateCountdownWithValues(data.days, data.hours, data.minutes, data.seconds);
                                } else {
                                    // Calculate values from end_date
                                    updateCountdown(endDate);
                                }
                                
                                // Update countdown every second
                                const interval = setInterval(() => {
                                    updateCountdown(endDate);
                                }, 1000);
                                
                                // Cleanup on page unload
                                window.addEventListener('beforeunload', () => {
                                    clearInterval(interval);
                                });
                            })
                            .catch(error => {
                                console.error('Error fetching countdown data:', error);
                                if (countdownLoading && countdownError) {
                                    countdownLoading.classList.add('hidden');
                                    countdownError.classList.remove('hidden');
                                }
                            });
                        
                        // Update countdown with values provided by the API
                        function updateCountdownWithValues(days: number, hours: number, minutes: number, seconds: number): void {
                            // Check if countdown has expired
                            if (days <= 0 && hours <= 0 && minutes <= 0 && seconds <= 0) {
                                if (countdownDisplay && countdownExpired) {
                                    countdownDisplay.classList.add('hidden');
                                    countdownExpired.classList.remove('hidden');
                                }
                                return;
                            }
                            
                            // Show countdown display
                            if (countdownDisplay) {
                                countdownDisplay.classList.remove('hidden');
                            }
                            
                            // Update DOM elements with leading zeros
                            if (daysElement) daysElement.textContent = days.toString().padStart(2, '0');
                            if (hoursElement) hoursElement.textContent = hours.toString().padStart(2, '0');
                            if (minutesElement) minutesElement.textContent = minutes.toString().padStart(2, '0');
                            if (secondsElement) secondsElement.textContent = seconds.toString().padStart(2, '0');
                            
                            // Add animation effect
                            const elements = [daysElement, hoursElement, minutesElement, secondsElement];
                            elements.forEach(el => {
                                if (el) {
                                    el.classList.add('countdown-pulse');
                                    setTimeout(() => {
                                        el.classList.remove('countdown-pulse');
                                    }, 500);
                                }
                            });
                        }
                        
                        // Calculate and update countdown based on end date
                        function updateCountdown(endDate: Date): void {
                            const now = new Date().getTime();
                            const timeRemaining = endDate.getTime() - now;
                            
                            if (timeRemaining <= 0) {
                                // Countdown has expired
                                if (countdownDisplay && countdownExpired) {
                                    countdownDisplay.classList.add('hidden');
                                    countdownExpired.classList.remove('hidden');
                                }
                                return;
                            }
                            
                            // Show countdown display
                            if (countdownDisplay) {
                                countdownDisplay.classList.remove('hidden');
                            }
                            
                            // Calculate time units
                            const days = Math.floor(timeRemaining / (1000 * 60 * 60 * 24));
                            const hours = Math.floor((timeRemaining % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                            const minutes = Math.floor((timeRemaining % (1000 * 60 * 60)) / (1000 * 60));
                            const seconds = Math.floor((timeRemaining % (1000 * 60)) / 1000);
                            
                            // Update with calculated values
                            updateCountdownWithValues(days, hours, minutes, seconds);
                        }
                    });
                </script>
                  <style>
                    /* Add animation for countdown */
                    @keyframes countdown-pulse {
                        0% { transform: scale(1); }
                        50% { transform: scale(1.05); }
                        100% { transform: scale(1); }
                    }
                    
                    .countdown-pulse {
                        animation: countdown-pulse 0.5s ease-in-out;
                    }
                    
                    /* Glow effect for countdown boxes */
                    #countdown-container .bg-white\/90 {
                        transition: all 0.3s ease-in-out;
                        box-shadow: 0 4px 20px rgba(64, 99, 134, 0.2);
                    }
                    
                    #countdown-container .bg-white\/90:hover {
                        box-shadow: 0 8px 25px rgba(64, 99, 134, 0.3);
                        transform: translateY(-2px);
                    }
                    
                    /* Pulse animation for seconds */
                    @keyframes seconds-pulse {
                        0% { box-shadow: 0 0 0 0 rgba(255, 255, 255, 0.4); }
                        70% { box-shadow: 0 0 0 10px rgba(255, 255, 255, 0); }
                        100% { box-shadow: 0 0 0 0 rgba(255, 255, 255, 0); }
                    }
                    
                    #seconds {
                        animation: seconds-pulse 2s infinite;
                    }
                    
                    /* Make the countdown responsive on small screens */
                    @media (max-width: 640px) {
                        #countdown-container .space-x-4 {
                            gap: 0.5rem;
                        }
                        
                        #countdown-container .text-2xl {
                            font-size: 1.5rem;
                        }
                    }
                </style>
                <div class="inline-flex items-center justify-center p-1 rounded-full bg-white/10 backdrop-blur-sm">
                    <a href="/daftar" class="bg-white text-[#406386] px-6 sm:px-8 py-3 sm:py-4 rounded-full font-semibold text-sm sm:text-base hover:bg-blue-50 transition-colors duration-200">
                        Daftar Sekarang
                    </a>
            </div>
        </div>
    </section>

  <!-- About Section -->
  <section class="py-12 sm:py-16 lg:py-20">
    <div class="container px-4 mx-auto sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto">
        <div class="p-8 transition-all duration-300 bg-white border shadow-lg sm:p-10 lg:p-12 border-slate-200/60 rounded-3xl hover:shadow-xl hover:border-slate-200">
          
          <!-- Header with icon and title -->
          <div class="flex flex-col items-start gap-6 mb-8 sm:flex-row sm:items-center sm:gap-8 sm:mb-10">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 sm:p-5 rounded-2xl shadow-md">
              <svg class="text-white w-7 h-7 sm:w-9 sm:h-9" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
              </svg>
            </div>
            
            <h3 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-[#406386] leading-tight">
              Tentang Program Beasiswa
            </h3>
          </div>
          
          <!-- Description -->
          <div class="space-y-4">
            <p class="text-base font-medium leading-relaxed sm:text-lg lg:text-xl text-slate-700">
              Program Beasiswa Bersekolah adalah inisiatif yang mendukung pelajar bersemangat dari tingkat SMP hingga SMA. 
            </p>
            <p class="text-sm leading-relaxed sm:text-base lg:text-lg text-slate-600">
              Kami percaya bahwa setiap anak berhak mendapatkan pendidikan berkualitas tanpa terkendala masalah finansial. Melalui program ini, kami berkomitmen untuk membuka akses pendidikan yang lebih luas bagi generasi penerus bangsa.
            </p>
          </div>
          
        </div>
      </div>
    </div>
  </section>

  <!-- Benefits Section -->
  <section class="py-12 sm:py-16 lg:py-20 bg-gradient-to-br from-blue-50 via-indigo-50 to-[#406386]/5">
    <div class="container px-4 mx-auto sm:px-6 lg:px-8">
      <div class="mb-8 text-center sm:mb-12 lg:mb-16">
        <h2 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-[#406386] mb-4 sm:mb-6">
          Manfaat Program Beasiswa
        </h2>
        <div class="w-16 sm:w-20 h-1 bg-[#406386] mx-auto rounded-full mb-4 sm:mb-6"></div>
        <p class="max-w-3xl px-2 mx-auto text-sm leading-relaxed text-gray-600 sm:text-base lg:text-lg">
          Program Beasiswa Bersekolah adalah inisiatif yang mendukung pelajar bersemangat dari tingkat SMP hingga SMA. Kami percaya bahwa setiap anak berhak mendapatkan pendidikan berkualitas tanpa terkendala masalah finansial.
        </p>
      </div>
      
      <div class="max-w-6xl mx-auto space-y-6 sm:space-y-8">
        <!-- Benefit 1 - Dukungan Finansial -->
        <div class="p-6 transition-all duration-300 bg-white border border-gray-100 shadow-lg rounded-xl sm:rounded-2xl sm:p-8 hover:shadow-xl group">
          <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:gap-6">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 rounded-xl group-hover:scale-105 transition-transform duration-200 flex-shrink-0">
              <svg class="w-6 h-6 text-white sm:w-8 sm:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
              </svg>
            </div>
            <div class="flex-1">
              <h4 class="font-bold text-[#406386] text-lg sm:text-xl mb-2">Dukungan Finansial</h4>
              <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
                Bantuan dana pendidikan sebesar <span class="font-semibold text-[#406386]">Rp 500.000/bulan</span> untuk mendukung kebutuhan belajar dan pengembangan akademik siswa.
              </p>
            </div>
          </div>
        </div>

        <!-- Benefit 2 - Pendampingan Mentor -->
        <div class="p-6 transition-all duration-300 bg-white border border-gray-100 shadow-lg rounded-xl sm:rounded-2xl sm:p-8 hover:shadow-xl group">
          <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:gap-6">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 rounded-xl group-hover:scale-105 transition-transform duration-200 flex-shrink-0">
              <svg class="w-6 h-6 text-white sm:w-8 sm:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
            </div>
            <div class="flex-1">
              <h4 class="font-bold text-[#406386] text-lg sm:text-xl mb-2">Pendampingan Mentor</h4>
              <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
                Pendampingan intensif dari <span class="font-semibold text-[#406386]">mentor berpengalaman</span> untuk pengembangan akademik dan personal melalui sesi konsultasi rutin.
              </p>
            </div>
          </div>
        </div>

        <!-- Benefit 3 - Pelatihan Soft Skill -->
        <div class="p-6 transition-all duration-300 bg-white border border-gray-100 shadow-lg rounded-xl sm:rounded-2xl sm:p-8 hover:shadow-xl group">
          <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:gap-6">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 rounded-xl group-hover:scale-105 transition-transform duration-200 flex-shrink-0">
              <svg class="w-6 h-6 text-white sm:w-8 sm:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
            </div>
            <div class="flex-1">
              <h4 class="font-bold text-[#406386] text-lg sm:text-xl mb-2">Pelatihan Soft Skill</h4>
              <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
                Akses ke berbagai <span class="font-semibold text-[#406386]">pelatihan pengembangan diri</span> seperti leadership, public speaking, dan keterampilan hidup lainnya.
              </p>
            </div>
          </div>
        </div>

        <!-- Benefit 4 - Jaringan Alumnus -->
        <div class="p-6 duration-300 bg-white border border-gray-100 shadow-lg transit ion-all rounded-xl sm:rounded-2xl sm:p-8 hover:shadow-xl group">
          <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:gap-6">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 rounded-xl group-hover:scale-105 transition-transform duration-200 flex-shrink-0">
              <svg class="w-6 h-6 text-white sm:w-8 sm:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
            </div>
            <div class="flex-1">
              <h4 class="font-bold text-[#406386] text-lg sm:text-xl mb-2">Jaringan Alumnus</h4>
              <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
                Bergabung dengan <span class="font-semibold text-[#406386]">komunitas beswan alumni</span> yang solid dan supportif untuk networking dan career development.
              </p>
            </div>
          </div>
        </div>

        <!-- Benefit 5 - Kunjungan Edukatif -->
        <div class="p-6 transition-all duration-300 bg-white border border-gray-100 shadow-lg rounded-xl sm:rounded-2xl sm:p-8 hover:shadow-xl group">
          <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:gap-6">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 rounded-xl group-hover:scale-105 transition-transform duration-200 flex-shrink-0">
              <svg class="w-6 h-6 text-white sm:w-8 sm:h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
              </svg>
            </div>
            <div class="flex-1">
              <h4 class="font-bold text-[#406386] text-lg sm:text-xl mb-2">Kunjungan Edukatif</h4>
              <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
                Kesempatan mengunjungi <span class="font-semibold text-[#406386]">kampus dan institusi terkemuka</span> untuk memberikan wawasan dan inspirasi tentang masa depan pendidikan.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Activities Section -->
  <section class="py-12 sm:py-16 lg:py-20">
    <div class="container px-4 mx-auto sm:px-6 lg:px-8">
      <div class="mb-8 text-center sm:mb-12 lg:mb-16">
        <h2 class="text-2xl sm:text-3xl lg:text-4xl font-bold text-[#406386] mb-4 sm:mb-6">
          Kegiatan Program Beasiswa
        </h2>
        <div class="w-16 sm:w-20 h-1 bg-[#406386] mx-auto rounded-full mb-4 sm:mb-6"></div>
      </div>
      
      <div class="grid max-w-6xl grid-cols-1 gap-6 mx-auto md:grid-cols-2 lg:grid-cols-3 sm:gap-8">
        <!-- Activity 1 -->
        <div class="relative p-6 overflow-hidden transition-all duration-500 bg-white border border-gray-100 shadow-xl group rounded-2xl sm:rounded-3xl sm:p-8 hover:shadow-2xl">
          <div class="absolute top-0 left-0 w-full h-1 bg-[#406386]"></div>
          <div class="flex flex-col items-center text-center">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 sm:p-6 rounded-full mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white sm:w-10 sm:h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m3 5.197V9a3 3 0 00-6 0v8.5l3-3"></path>
              </svg>
            </div>
            <h3 class="text-xl sm:text-2xl font-bold text-[#406386] mb-3 sm:mb-4">Bertemu</h3>
            <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
              Kegiatan pembuka yang menjadi wadah <span class="font-semibold text-[#406386]">pertemuan pertama</span> antara para beswan dan mentor untuk membangun koneksi yang kuat.
            </p>
          </div>
        </div>

        <!-- Activity 2 -->
        <div class="relative p-6 overflow-hidden transition-all duration-500 bg-white border border-gray-100 shadow-xl group rounded-2xl sm:rounded-3xl sm:p-8 hover:shadow-2xl">
      <div class="absolute top-0 left-0 w-full h-1 bg-[#406386]"></div>
          <div class="flex flex-col items-center text-center">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 sm:p-6 rounded-full mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white sm:w-10 sm:h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
              </svg>
            </div>
            <h3 class="text-xl sm:text-2xl font-bold text-[#406386] mb-3 sm:mb-4">Bertumbuh</h3>
            <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
              Program <span class="font-semibold text-[#406386]">pengembangan diri</span> berupa kelas soft skill yang diselenggarakan setiap bulan untuk mengasah kemampuan.
            </p>
          </div>
        </div>

        <!-- Activity 3 -->
        <div class="relative p-6 overflow-hidden transition-all duration-500 bg-white border border-gray-100 shadow-xl group rounded-2xl sm:rounded-3xl sm:p-8 hover:shadow-2xl md:col-span-2 lg:col-span-1">
          <div class="absolute top-0 left-0 w-full h-1 bg-[#406386]"></div>
          <div class="flex flex-col items-center text-center">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 sm:p-6 rounded-full mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white sm:w-10 sm:h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
              </svg>
            </div>
            <h3 class="text-xl sm:text-2xl font-bold text-[#406386] mb-3 sm:mb-4">Bercerita</h3>
            <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
              Sesi <span class="font-semibold text-[#406386]">reflektif</span> yang memberikan ruang bagi beswan untuk berbagi kisah, pengalaman, dan pembelajaran hidup.
            </p>
          </div>
        </div>

        <!-- Activity 4 (Berolahraga) -->
        <div class="relative p-6 overflow-hidden transition-all duration-500 bg-white border border-gray-100 shadow-xl group rounded-2xl sm:rounded-3xl sm:p-8 hover:shadow-2xl md:col-span-2 lg:col-span-1">
          <div class="absolute top-0 left-0 w-full h-1 bg-[#406386]"></div>
          <div class="flex flex-col items-center text-center">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 sm:p-6 rounded-full mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white sm:w-10 sm:h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7zM7 9h.01M12 9h.01M17 9h.01M7 14h.01M12 14h.01M17 14h.01"></path>
              </svg>
            </div>
            <h3 class="text-xl sm:text-2xl font-bold text-[#406386] mb-3 sm:mb-4">Berolahraga</h3>
            <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
              <span class="font-semibold text-[#406386]">Aktivitas fisik</span> yang dilaksanakan secara berkala guna menjaga kesehatan jasmani dan membangun semangat kebersamaan.
            </p>
          </div>
        </div>

        <!-- Activity 5 (Berkunjung) -->
        <div class="relative p-6 overflow-hidden transition-all duration-500 bg-white border border-gray-100 shadow-xl group rounded-2xl sm:rounded-3xl sm:p-8 hover:shadow-2xl md:col-span-2 lg:col-span-1">
          <div class="absolute top-0 left-0 w-full h-1 bg-[#406386]"></div>
          <div class="flex flex-col items-center text-center">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 sm:p-6 rounded-full mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white sm:w-10 sm:h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
              </svg>
            </div>
            <h3 class="text-xl sm:text-2xl font-bold text-[#406386] mb-3 sm:mb-4">Berkunjung</h3>
            <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
              Program <span class="font-semibold text-[#406386]">kunjungan edukatif</span> ke berbagai institusi, perusahaan, dan ruang kreatif.
            </p>
          </div>
        </div>

        <!-- Activity 6 (Beribadah) -->
        <div class="relative p-6 overflow-hidden transition-all duration-500 bg-white border border-gray-100 shadow-xl group rounded-2xl sm:rounded-3xl sm:p-8 hover:shadow-2xl md:col-span-2 lg:col-span-1">
          <div class="absolute top-0 left-0 w-full h-1 bg-[#406386]"></div>
          <div class="flex flex-col items-center text-center">
            <div class="bg-gradient-to-br from-[#406386] to-[#2d4a67] p-4 sm:p-6 rounded-full mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
              <svg class="w-8 h-8 text-white sm:w-10 sm:h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7.5V9M6 7.5V9M3 13.5h18"></path>
              </svg>
            </div>
            <h3 class="text-xl sm:text-2xl font-bold text-[#406386] mb-3 sm:mb-4">Beribadah</h3>
            <p class="text-sm leading-relaxed text-gray-600 sm:text-base">
              Kegiatan <span class="font-semibold text-[#406386]">spiritual</span> tahunan yang bersifat opsional, seperti itikaf bersama.
            </p>
          </div>
        </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="relative bg-gradient-to-br from-[#406386] to-[#2d4a67] py-12 sm:py-16 lg:py-20 overflow-hidden">
    <!-- Subtle background pattern -->
    <div class="absolute inset-0 opacity-5">
      <div class="absolute w-20 h-20 bg-white rounded-full top-10 right-10 blur-2xl"></div>
      <div class="absolute w-16 h-16 bg-white rounded-full bottom-10 left-10 blur-xl"></div>
    </div>
    
    <div class="container relative z-10 px-4 mx-auto sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center">
        <!-- Heading -->
        <h2 class="mb-4 text-2xl font-bold leading-tight text-white sm:text-3xl lg:text-4xl sm:mb-6">
          Siap Memulai Perjalananmu?
        </h2>
        
        <!-- Description -->
        <p class="max-w-2xl mx-auto mb-8 text-base leading-relaxed text-blue-100 sm:text-lg lg:text-xl sm:mb-10">
          Bergabunglah dengan ribuan pelajar lainnya yang telah merasakan manfaat Program Beasiswa Bersekolah
        </p>
        
        <!-- Single CTA Button -->
        <div class="mb-8 sm:mb-10">
          <a href="/masuk" class="inline-block group">
            <button class="bg-white text-[#406386] px-8 sm:px-10 py-3 sm:py-4 rounded-full font-semibold text-base sm:text-lg hover:bg-blue-50 transition-all duration-300 shadow-lg hover:shadow-xl hover:-translate-y-0.5">
              Daftar Beasiswa Sekarang
            </button>
          </a>
        </div>
        
        <!-- Trust indicator -->
        <div class="flex items-center justify-center space-x-4 text-sm text-blue-200/80">
          <span>1000+ pelajar terdaftar</span>
        </div>
      </div>
    </div>
  </section>
</AppLayout>